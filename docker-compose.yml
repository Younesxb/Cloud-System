version: '3'
services:
  php:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html:ro
  db:
    image: mariadb:latest
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: YNSGaming
      MYSQL_USER: root


