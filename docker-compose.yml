version: '3'
services:
  php:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html:ro
  db:
    image: mariadb:latest
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: YNSGaming
      MYSQL_USER: root
      MYSQL_PASSWORD: Younes22
    ports:
      - "3306:3306"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2.5.0

